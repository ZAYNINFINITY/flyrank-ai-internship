/* Shared warm-editorial palette used across Foyer's flat pages.
   Mirrors the 2D engine room (operational-room.tsx) so every surface
   reads as one museum instead of a set of mismatched templates. */
export const S = {
  bg: "#F5F0E7",
  surface: "#F8F3E9",
  text: "#2E2821",
  accent: "#C94F0A",
  border: "rgba(46,40,33,0.12)",
  muted: "rgba(46,40,33,0.45)",
} as const;
